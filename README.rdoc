== Rest_prev
  提高手机软件的开发效率，减少客户端与服务器端开发过程中的耦合。

=== Todo
* 增加guard支持

=== Done
* 实现功能
* 确定解决方案也页面UI
* 增加bootstrap支持
* 需要根据参数和方法，限制api的请求
* 页面还需要优化
* 增加rspec测试

=== 功能点
* 能动态的增加接口
* route访问能映射到指定的接口
* 能设置默认的接口定义
* 能新建不同项目的接口定义

